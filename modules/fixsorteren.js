export default function sorteren(waarde1, waarde2) { // sort() werkt op alfabetische volgorde, dus 1  == 10 waardoor deze bij elkaar komen te staan
    if (waarde1 < waarde2) { // Nu zal de array data gezien worden als een getal en dus wel in volgorde worden gezet
        return -1;
    }
}